# Hazard Quantization Companion Site

Static Next.js app for the FQ-IDCVT paper companion site.

```bash
npm install
npm run lint
npm run build
```

Notebook figures and tables are expected under `public/figures/` and
`public/data/`. Missing assets render as pending states.
